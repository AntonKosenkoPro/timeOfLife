# Entry Text & Categories — FURPS

Requirements for **entry-owned text and category tags** (OpenSpec change `remove-activities-layer`): there is no activity entity. Every entry owns its trimmed `activity_text`, its ordered `category_ids`, and its `notes` at write time; history never mutates retroactively. The Track screen offers a plain-text field plus recency chips computed on-device from past entries.

App-wide requirements (minimalism, dark/light, offline, EN+RU localization, HIG, secure storage, linters, tests, CI, OpenAPI, `AGENTS.md`) live in [`Common.md`](./Common.md) and are not repeated here; only feature-specific rows are listed.

| Index | Requirement                                                                                                                                                             | Comment                                                                                                                                                                                                               |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1    | Entries own their fields: trimmed `activity_text`, ordered `category_ids`, and `notes` are written onto the entry at creation and never change unless that entry is edited. There is no activity record, no `activity_id`, and no query-time resolution. | Identity is the exact trimmed text, byte-exact and case-sensitive (`Gym` ≠ `GYM`). Editing one entry never changes another entry (per-entry isolation). |
| F2    | Create, edit, and delete **categories** from Profile's Manage Categories destination. A category has a validated name and a catalog SF Symbol icon.                                  | A tag layer; an entry may carry zero or more category tags in an explicit order. The local catalog remains usable signed out and offline.                                                                                 |
| F3    | Tag/untag an entry with zero or more categories in an explicit order; the first-position category supplies the chip/row icon.                                                                                                                      | Tags are optional; an entry with no tags is valid (ad-hoc / uncategorized tracking remains possible). Order is set at write time and preserved. |
| F4    | Starting a timer by entering free text just works: an exact match against recent entries inherits that entry's full ordered category set, otherwise the draft starts with no categories. Start is gated on trimmed non-empty text.                                          | No catalog lookup, no auto-created records, no reuse-or-duplicate question — the text IS the identity. Categories can be attached before Start via the running tag selector or baked at Stop. |
| F5    | The Track screen shows **Recents**: the user's up-to-6 most recently used exact texts, computed **on-device** from committed entries (`GROUP BY activity_text`, newest `started_at` wins per group, `id DESC` tiebreak).                                  | No server endpoint (works fully offline). One tap fills the text field (ready state) and inherits that entry's ordered categories. Recents is a wrapping chip flow (no horizontal scrolling) hidden while the timer is running; chips show the icon of the first-position Category only, and the prepared chip uses a filled accent highlight. |
| F6    | On first local-dataset setup, a **default starter set of 7 categories** is seeded exactly once, localized in EN and RU.                                          | Categories: Work, Hobby, Sport, Education, Relax, Sleep, Entertainment. A durable local marker prevents reseeding after user deletion; erase-local-data clears the marker. No starter entries are seeded. Seeds are ordinary records — fully editable and deletable. |
| F7    | (Removed by `remove-activities-layer`.) The Track Activity search sheet and quick-create no longer exist; capture is a plain-text field plus Recents (F4/F5). | Track has no editing affordance for past entries; history is corrected from the entry form (F10). |
| F8    | (Removed by `remove-activities-layer`.) There is no Manage Activities screen and no activity CRUD. Category CRUD lives on the separate Manage Categories screen. | Reached from Profile. The categories list is ordered by name. |
| F9    | Entries carry `activity_text`, ordered `category_ids`, and `notes` denormalized at write time. | The OpenAPI spec (`backend/api/openapi.yaml`, S10) and the offline sync contract carry entries with owned fields; there is no `/activities*` endpoint and no `activity_id` anywhere. Nothing is resolved at query time. |
| F10   | Deleting an entry from its entry form confirms the scope: the prompt names the entry text. Per-entry deletion lives on the entry form only. | The deletion is destructive and triggers the undo flow (R3). There is no cascade and no scope choice — one entry, one delete. |
| F12   | Entries and categories are **synced across devices** and available offline.                                                                                          | CRUD performed offline is queued locally and synced when connectivity returns (D7).                                                                                                                                   |
| U1    | Validation: entry/category name is non-empty and ≤ 60 chars; notes ≤ 280 chars; category icons are chosen from the provided set.                                    | Mirrors the auth validators' pattern (one field → one error).                                                                                                                                                         |
| U2    | Validation errors are shown directly beneath the editor, and multiple rules for one field collapse into a single unified message.                                       | Matches `Sign-up_and_Sign-in` U2/U4 and design decision D4.                                                                                                                                                            |
| U3    | Recents render as a wrapping flow of tappable chips (cap 6) with the exact entry text and the first-position Category's icon only; Category names never appear on capture. Selecting one fills the text field and inherits that entry's ordered categories. | Stable accessibility identifiers: `TimerSuggestion(<text>)`; the prepared chip announces its selected state.                                                                         |
| U4    | The entry form follows the keyboard/placement rule (primary input in the upper scroll area, primary action pinned to `.safeAreaInset`).                            | Design decision D13. The entry form's Save bar is pinned via `.safeAreaInset(edge: .bottom)`. |
| U5    | Deletion presents a clear destructive confirmation naming the entry text.                                                                 | The confirm button uses a destructive role/tint.                                                                                                                                                                      |
| U6    | Every implemented entry/category delete is undoable until the app restarts through the DEFAULT system Undo confirmation (shake → prompt → confirm restores the most recent deletion). No toast is shown.                                                                      | Entry and category deletions enter the durable undo buffer before relay deletion; each surface filters by snapshot resource (U7 supersession across surfaces).                                                                                       |
| U7    | **Shake-to-undo** (iOS system Undo gesture) restores the most recent undoable deletion on the presenting surface: entry deletions on History/entry form, category deletions on Manage Categories.                                  | Uses the system motion event and `UndoManager` with a passive first-responder host; registrations are cleared-then-single and resource-filtered, so one shake+confirm restores at most one deletion.                                                                                                                                                                            |
| U8    | Empty states are shown when there are no recents / no categories.                                                | Empty states guide toward creation and never block capture — the Track field explains that typing a name and starting always works (no separate alert). |
| U9    | All user-facing strings use `L10n.*` keys (EN + RU); visual tokens use `Theme.*`; native SwiftUI components only; stable `accessibilityIdentifier`s on every control. | Common U2/U4/U5 and design decisions D3/D4/D6.                                                                                                                                                                         |
| R1    | Entry/category CRUD is **offline-first**: works with no connectivity and syncs on reconnect.                                                                        | Design decision D7 / Common U3.                                                                                                                                                                                       |
| R2    | Sync conflicts (same entry edited on two devices) resolve by **last-write-wins on `updated_at`**.                                                                    | The most recent write per record wins; no field-level merge at MVP.                                                                                                                                                |
| R3    | Deletions are **atomic and undoable**: a deletion is held in a client-side undo buffer and is not committed to the local store or pushed to sync until the app restarts (a cold launch commits whatever is still buffered). | Shake-to-undo re-inserts from the same buffer. On restart, buffered deletions are committed locally and synced; the server performs a hard delete (no long-lived trash).                                              |
| P1    | Recents ranking returns in < 50 ms on-device for entry tables up to ~10 000 rows.                                                                                  | Computed locally from the entries table via the `(activity_text, started_at)` index; no network round-trip (F5).                                                                                                                       |
| P2    | The History list scrolls at 60 fps with large entry counts.                                                                                                       | Lazy loading / `LazyVStack`; no work in view body.                                                                                                                                                                     |
| S1    | New backend endpoints are documented in `backend/api/openapi.yaml` (OpenAPI 3.0).                                                                                       | Common S10.                                                                                                                                                                                                           |
| S2    | Unit tests cover validators, the outbox, exact-text identity (`Gym` ≠ `GYM`), per-entry isolation, the prune-unknown-category rule, and stop-only entry creation.                               | Common S3; logic-layer ~100%.                                                                                                                                                                                         |
| S3    | `AGENTS.md`, the API contract section, and `Design/` (timer capture spec, entry form, History, Insights) are updated.       | Common S5/S7.                                                                                                                                                                                                         |
| S4    | Structured `log/slog` logging for entry/category CRUD events; no PII (notes content) in logs.                                                                        | Common S8 / R1 posture.                                                                                                                                                                                                |

[^1]: The typed text is a first-class identity: it appears in Recents next time and inherits its categories on exact match. The user is never forced to open a catalog to start a timer — typing a name and starting still works in one step (F4).

## Resolved decisions

- **Sync conflict policy** (R2): last-write-wins on `updated_at`; no field-level merge at MVP.
- **Seed set** (F6): 7 categories — Work, Hobby, Sport, Education, Relax, Sleep, Entertainment (EN/RU). No starter entries.
- **Limits**: no count caps enforced at MVP; name ≤ 60 chars, notes ≤ 280 chars.
- **No activity catalog** (`remove-activities-layer`): F7/F8 removed; there is no activity search, quick-create, Manage Activities, or activity editor anywhere in the app.
