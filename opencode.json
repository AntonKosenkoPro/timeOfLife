{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "AGENTS.md"
  ],
  "agent": {
    "explore": {
      "description": "Fast read-only codebase explorer. Use PROACTIVELY instead of reading/searching files yourself: any question spanning 2+ areas (backend vs iOS vs specs/docs) or needing file/pattern searches MUST be fanned out as 2-3 parallel explore Task calls in ONE message, one per disjoint area. Explore-only work stays at 3 agents max.",
      "model": "ollama-cloud/glm-5.3-flash",
      "reasoningEffort": "high"
    },
    "build": {
      "model": "ollama-cloud/deepseek-v4-flash",
      "reasoningEffort": "max"
    },
    "general": {
      "description": "General-purpose worker for independent units of work. Use PROACTIVELY instead of working sequentially: any task touching 2+ of backend/ vs ios/TimeOfLife/ vs specs-docs MUST be split into parallel Task calls (one per area, single message) before doing anything yourself. Backend verify and iOS verify always run on separate agents in parallel. Default fan-out 4 agents, hard ceiling 6.",
      "model": "ollama-cloud/glm-5.3-flash",
      "reasoningEffort": "high"
    },
    "plan": {
      "model": "opencode-go/kimi-k3",
      "reasoningEffort": "max",
      "permission": {
        "argent*": "deny"
      }
    }
  },
  "mcp": {
    "argent": {
      "type": "local",
      "command": [
        "argent",
        "mcp"
      ],
      "enabled": true
    }
  },
  "subagent_depth": 2,
  "tools": {
    "argent*": true
  }
}
